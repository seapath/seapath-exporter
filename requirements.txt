# prometheus-client 0.26.0 is the first release exposing tls_min_version.
prometheus-client==0.26.0
# Left unpinned on purpose: libvirt-python is built from source against the
# libvirt headers of the base image.
libvirt-python>=10.0.0
psutil==5.9.8
